# MYOURISCOPE プロダクト仕様書

> 妙理(みょうり)= 言葉にしがたい、ものごとの奥にある理 × scope(観測器)。
> 本番: https://myouriscope.com/ (GitHub Pages / 純粋な静的サイト・サーバーなし)

最終更新: 2026-09-07

---

## 1. コンセプトと思想

- **占いは、決定ではなく観測。** 断定しない・脅さない・買わせない。毎朝そっと背中を押す。
- **1日1回の楽しみを守る。** 今日の一枚は1日1回だけ。引き直し不可。
- **結果のネタバレをしない。** 未観測の日の結果は再訪FVに表示しない。
- **根拠を見せる。** スコア・結論には必ず「どう計算したか」を添える(票の内訳、計算式、称号の由来)。
- **データは端末の中だけ。** 生年月日・履歴・図鑑はすべて localStorage。サーバー送信なし。
- 商業的な煽り表現(「無料」等)は使わない。

## 2. 技術構成

| 項目 | 内容 |
|---|---|
| 構成 | `index.html` + `css/style.css` + `js/{data,fortune,icons,mystic,app}.js`(フレームワークなし) |
| ルーティング | ハッシュ(`#tarot` 等)+ pushState/popstate。リロード・戻るで画面保持 |
| ホスティング | GitHub Pages。`.github/workflows/deploy-pages.yml` がプッシュごとにデプロイ |
| キャッシュ | ワークフローが `?v=dev` をコミットSHAに置換(キャッシュバスト)。SWはネットワーク優先 |
| ドメイン | myouriscope.com(お名前.com / NS: 01-04.dnsv.jp / A: 185.199.108-111.153, www CNAME hidaka86.github.io) |
| PWA | manifest.json + sw.js(ホーム画面追加・オフラインフォールバック) |

### デプロイの注意
- Pages の一時エラー「Deployment failed, try again later」対策として、ワークフロー内に sleep 70 + リトライあり。それでも落ちたら **rerun ではなく workflow_dispatch で新規実行**(rerun はアーティファクト重複で失敗する)。
- カスタムドメインのAPI自動設定は `PAGES_ADMIN_TOKEN`(fine-grained PAT / Pages: write)がシークレットにある場合のみ実行。なければ手動(Settings → Pages → Custom domain)。

## 3. 画面と導線

| 画面 | 内容 |
|---|---|
| ホーム | FVは「今日の占い」への導線のみ(btn-observe)。観測カードの上に「なにを、知りたいですか?」(`renderWants`、7つの知りたいこと→おすすめ1〜3件と理由・必要なもの・所要時間、向いているカードだけ灯る、七つの占いの比較表)。観測カードは8枚(今日・タロット・ホロスコープ・四柱推命・相性・統合・ダイス・マヤ)、アイコンは全て assets/site の線画(ダイス・マヤはSVG)。再訪者は当日の観測状況で出し分け(未観測=ネタバレなしの誘い/観測済み=結論・スコア・連続日数のコンパクト表示+カウントアップ) |
| 今日の占い | カードを引く→結論(大吉〜休)→気流メーター→ラッキー→マインド(結び)→シェア |
| タロット | 目的で選ぶスプレッド(ワンオラクル/3枚/ケルト十字)→儀式(章参照)→結果 |
| ホロスコープ | 出生図+今日の空+5年周期+今年+テーマ別リーディング |
| 四柱推命 | 命式+今日/今週/今月/12ヶ月の気流+九星・吉方位(マイページ) |
| 相性診断 | 4層スコア+見え方(非対称)+取扱説明書+シェア |
| 統合鑑定 | 運命の称号(1080)+由来分解+西×東の重ね読み |
| アストロダイス | 天体12・星座12・ハウス12の三つの12面体(CSS 3D)。全画面「ダイスの間」で卓を押さえて・振って・はなすと物理演算で転がり、出目を上にして止まる→読む順に整列→三つの目を順に灯す→一行の答え→鑑定。出目は手をはなした瞬間の crypto.getRandomValues。問い(任意)を添えられる |
| マヤ暦 | 生年月日→KIN(ツォルキン260日)。あなたという人(紋章の資質+音の役割+13日の段階)・太陽の紋章(持ち味/つまずき/色の意味)・銀河の音(役割/コツ)・ウェイブスペル・運命のオラクル(ガイド/類似/反対/神秘)・絶対反対キン・鏡の向こうのKIN・今日のKINとの関係と過ごし方・お相手(任意)は「二人のツォルキン」:縁の名前・関係KIN(二人のKINの和を260で折り返し)・ツォルキンの環SVG(260日の環に二人と関係KINを置き、誕生KINの距離を表示)・四つの視点(安心感/刺激/波長/分担、根拠つき)・紋章の関係を恋愛で読む(恋のかたち/ケンカの火種/仲直りのコツ、11種×双方向)・二人のオラクル(相手が自分のどの位置か)・音のリズム(6種)と分担・色の組み合わせ(7種)・次の「二人の日」(関係KINが次にめぐる日)・二人で過ごすなら(20種)・今日の二人・シェア画像 |
| 読みもの | 解説記事5本(タロット/ホロスコープ/四柱推命と九星/相性/思想) |
| マイページ | ヘッダー右上の人物アイコン。占い結果ゾーン+「ARCHIVE ここから下は、あなたの記録」(称号図鑑・アプリ追加・引き継ぎ・履歴) |

## 4. タロット

- **フルデッキ78枚**: 大アルカナ22(`tarot_00_fool.webp` 等)+小アルカナ56(`tarot_{suit}_{rank}.webp`、suit=wands/cups/swords/pentacles、rank=ace,02..10,page,knight,queen,king)。全カードに正位置/逆位置/助言テキスト。
- **スプレッド**(`RITUAL_SPREADS`): 今日の一枚(トップ専用・1日1回)/ワンオラクル(1枚・目の前のこと)/3枚(過去現在未来)/ケルト十字(10枚・DEEP)。
- **スプレッド選択UI**: 「夜の窓」カード(背景に対応アルカナ: ワンオラクル=魔術師、3枚=運命の輪、ケルト=月)。**タップ=即儀式開始**の1工程。
- **シャッフル(2方式・端末に保存 `fortuna:shufmode`)**:
  - **散らして選ぶ**(デフォルト): 34枚が全画面に散らばり、指の軌跡が物理演算(慣性+摩擦+常時ゆらぎ+カード同士の反発)で札を押し流す。タップでピック、規定枚数で自動リビール。
  - **重ねて混ぜる**: 長押しシャッフル(長押し時間とこする速さで加速)→3山カット→帯カルーセルからドロー。
- **シード思想**: 乱数は機械任せにしない。指を離した時刻・カットの選択・引いた位置・かき混ぜの軌跡を混ぜて `seededRng` に投入。
- **結果**: 結論カード「つまり、こういうこと」(word/reason/action)→各カード詳細(正逆両義)→デッキ深読み(大アルカナ比・スート偏り・正逆比・組み合わせ)→シェア。
- **リビール**: タップで早送り、SKIP» ピルあり。1画面に収まる dvh 設計。

### タロット・緩急の仕掛け(エンゲージメント設計)

> 設計原則: **急がせる理由は「アプリの都合」ではなく「世界観(空)の都合」に置く。** 引き直し課金・FOMOカウントダウン等の不安につけ込む形は採らない。

**のんびり側(深さ・熟成)**
- **呼吸シャッフル**: 「重ねて混ぜる」長押し中に「吸って→とめて→吐いて」の呼吸ガイド。1呼吸(約9.4秒)以上で `ritual.deepShuffle`、結果に「☽ 深く混ぜられたデッキから」の印。
- **月の間**(`RITUAL_SPREADS.night`): 22:00〜3:59だけ開く3枚スプレッド(手放すこと/夜の本音/明日への灯)。昼はロック表示で「今夜22:00にひらく」。一夜に一回(深夜0〜3時は前夜と同じ夜として `nightWindow().key` で判定)。儀式の間はさらに深い夜(`.ritual-night`)。
- **熟成する託宣**: ジャーナルの各記録は 12時間後に「ふたつめの意味」(先頭札の逆側の顔)、3日後に「振り返りの問い」(自分で意味づけする入力欄)がひらく。1回の抽選を3回の再訪に変える。
- **振り返りの呼びかけ**: 7日以上前で振り返り未記入の記録があると、タロット画面に「◯日前、あなたは『塔』を引きました」カードを表示。

**急ぎ側(希少性・旬)**
- **宙の窓**(`celestialWindows()`): 新月の日だけ「新月の窓」(はじまりの一枚)、満月の日だけ「満月の窓」(手放しの一枚)が出現。締切は月齢=「この窓は今夜まで」。1日1回、消化後はロック表示。
- **消える託宣**: 読みの言葉(結論・理由)は**24時間で薄れる**。ひとこと書き残す(memo)と定着。薄れた記録は札の名前だけが残る。
- **風のざわめき**(`windCallToday()`): `seededRng(日付+"|windcall") < 0.125`(年約46日)で全員共通に発生。当日はホーム/タロットにバナー、読みの結果に「ざわめきの一枚」(日付+出た札シードのボーナス1枚。再表示でも同じ札=スロット化させない)。
- **水星逆行**(`isMercuryRetrograde()`): 今日と明日の水星黄経の差で判定。逆行中は問いかけ画面に注意書き。

**週のリズム**(`tarotTempoLine()`): 平日=ワンオラクル推し/金土19時以降=ケルト十字推し(「きょうの誘い」バッジ)/日曜=ジャーナルの読み返し推し/22時以降=月の間の案内。毎日同じテンポにしない。

### タロット・ジャーナル(託宣の記録)
- `fortuna:journal`(最大60件)。エントリ: `{id, t, spread, genre, q, cards, word, reason, memo, reflection}`。
- 状態遷移は `journalState()`: 薄れ(24h・memoで回避)/ふたつめの意味(12h)/振り返り(72h)。
- スプレッド完了時に自動保存(今日の一枚は today 画面が担当するため対象外)。結果画面下に「いま、書き残しておく」導線。

## 5. 今日の占い(今日の結論)

`dailyVerdict(birthdate)` — 独立した4手法の「票」を集計:

1. **四柱推命・日運**(±2〜0): 日主×今日の日干の通変星。星の意味(gloss)+過ごし方まで解説。
2. **四柱推命・月運**(±1): 月全体の気流の説明つき。
3. **干支の巡り**(±2): 今日の日支×生まれ年支。三合/支合=+2、冲=−2、同支=+1。関係の解説つき。
4. **月相**(±1): 満ちる時期=始める、欠ける時期=手放す。

合計→ 大吉(+4↑)/吉/平/静/休。大吉と休には「年に数回」のレア度コピー。
**マインド言葉**は約160種(ベース: 大吉12/吉16/平24/静14/休12+各ランクにテーマ別4種×4テーマ)。ボリュームゾーンの「平」を最も手厚く。シードは日付×生年月日×カード×トップテーマ。さらに直近10回に表示した言葉はlocalStorage(`ms_mind_history`)に記録して候補から除外し、「また同じこと言ってる」を防ぐ(同日内はリロードしても同じ言葉)。ホーム観測済みFVのCTA文言も日替わり4種。

## 6. ホロスコープ

- **天体計算**: Paul Schlyter 近似(`planetLongitudes`)。水星〜海王星の軌道要素+木星/土星/天王星の摂動+冥王星の三角級数+月6項。精度±1°(娯楽用途に十分)。
- **出生図**: 10天体×星座+アスペクト(orb: 太陽月8/5° 他6/4°、上位7件)。
- **アセンダント**: 出生時刻×出生地(47都道府県 `PREF_GEO`)から GMST→地方恒星時→標準公式で計算。**日の出時に太陽黄経と一致することで検証済み**(誤差≒大気差1〜2°)。
- **今日の空**(毎日変わる): トランジットの月星座(`MOON_TODAY_DESC` 12種)+今日の天体×出生図のアスペクト上位3件+明日の月移動予告。
- **5年周期の流れ**: 土星の対出生土星象限(いつ)×トランジット土星のソーラーハウス=主戦場(どこで・生まれ月で12通り)×テーマ担当天体のサイン=あなたの型(どう進むか)。**同世代でも本文が変わることを1990年生8人で検証(7-8/8ユニーク)**。
- **今年の星模様**: 木星・土星のソーラーハウス(太陽星座起点の12部屋)。

## 7. 四柱推命・九星気学

- 三柱(年月日)+節入り簡易表。日主=日柱の干。
- **通変星**(`TSUHENSEI`): 10種それぞれに gloss(星の意味)/day/month テキスト+テーマ別重み。
- 「今日の気流」(日運・毎日変わる)/一週間/今月今年/12ヶ月(テーマ別ベスト月)。
- 九星: 本命星の気質+月盤の吉方位(五黄殺・暗剣殺・本命殺は除外)。

## 8. 相性診断

- **4層合算**: 星座エレメント30% + 九星五行25% + 干支(三合/支合/冲)25% + **日主の対話20%**(生まれた日の十干同士の通変星 `AISHO_BOND` — 同学年でも生まれ日で変わる)。
- **見え方(非対称)**: 五行の向き5関係 × 通変星10 = **50ラベル**(`PERSPECTIVE_LABELS`)+ニュアンス文10種。スコアも絆の質で±5揺れる。
- **一言キーワード**: バンド×関係ごとのプールからペア固有ハッシュで決定的に選択(25種、同じペアは常に同じ言葉)。
- **ふたりの取扱説明書**: 遊び方(エレメント組合せ10種 `AISHO_PLAY`)/ケンカの火種(最低点の層から `BOND_FRICTION`)/ふたりの吉日(向こう30日の日支×両者の年支を実スキャン)。
- **シェア画像**: 交わるふたつの円+中央スコア+非対称ラベル2行(1080×1920ストーリーズ)。

## 9. 統合鑑定と称号

- **運命の称号**: `SHOGO_NIKKAN`(日主の詩的言い換え: 大地・霊峰・鋼・野の花・慈雨など)×星座の枕詞12×九星の役割9 = **1080通り**。形式「◯◯を宿す、◯◯の◯◯」。
- 称号の下に**由来分解カード**(言葉←占術←意味)を表示。
- 称号図鑑(コレクション)・招待リンク(シェアURL経由で相手の図鑑に登録)。

## 9b. アストロダイスとマヤ暦(`js/extra.js`)

- **アストロダイス**: `FACES` に12面体の各面の向き(上面1・上段5・下段5・下面1、上段の極角63.435°・下段116.565°)を持ち、CSS `preserve-3d` + `clip-path` の五角形で正十二面体を組む。辺の長さは CSS 変数 `--edge`(卓上52px/モバイル38px、ダイスの間は画面に応じて34〜58px)。
  - **1日3回**(`fortuna:dice` = {date,count,last}): 放った瞬間に消費。使い切ると入口が閉じ「今日、最後に出た目を見る」で再表示(履歴には積まない)。
  - **儀式の三段**: 扉(問いを記す・残り度数を漢数字で告げる)→ 呼吸(2.6秒×3拍、1拍目から「となえました」で先へ)→ 卓。卓では押さえてから1.5秒「祈りが満ちる」まで包む(環が満ちる)。満ちる前に離すと目は卓に戻り消費しない。最初の目が定まる瞬間に1.4秒のスローモーション+周辺減光。
  - **ダイスの間**(`#ritual-overlay` を `dice-chamber` として流用): 卓(`.dice-felt`)を pointerdown で「手に取る」→ 指の動きで energy が溜まり震える → pointerup の直前90msの指速度で投げる(ほぼ静止なら手のひらからこぼす)。物理は固定16msステップ(x,y,vx,vy + 高さz,vz + 回転 rx,ry,rz)、卓の壁で反発、ダイス同士は円で衝突。速度と角速度が閾値を下回ると出目の面(`rotateX(-(90-φ)) rotateY(-az)`、最短の同値角へ)にイージングで収束(`settle`)。3つ止まったら読む順(天体・星座・ハウス)の3スロットへ900msで整列→1.25秒おきに灯してキャプション→一行の答え+「鑑定を読む/もう一度振る」。タップで早送り。`prefers-reduced-motion` は物理を省いて即整列。「手を使わずに振る」ボタンでキーボード/タップ困難でも振れる。
  - **読み**: 一行の答えは4文型を出目で決定(`diceReading`)。天体=what/advice/ask(自分への問い)、星座=how/mode/tip+エレメントのテンポ、ハウス=where/action(今日の一手)。
- **間柄別スコア**(`sceneScores`): 恋人/結婚・長い関係/仕事仲間/友人の4間柄で、基礎点42+関係名(`REL_SCENE`、片側だけ名前がつく場合はその側、両側なら平均)+色(`COLOR_SCENE`)+音のリズム(`RHYTHM_SCENE`)の固定加点、30〜98に丸める。反対キン・絶対反対キンは仕事仲間が高く恋人・結婚が低い、神秘キンは恋人が高く結婚が低い、類似キンは結婚が最高、など間柄で逆転する設計。4軸レーダーSVG+タイル(BEST表示・段階ラベル・内訳の折りたたみ)+見出し文(`sceneHeadline`)。シェア画像にはBESTの間柄と点を載せる。
- **二人のツォルキン**(`pairHtml`): 関係名は `relationOf` を双方向で取り、片側だけ名前がつく場合は名前のある側を主役にする(「名前なし」の文を混ぜない)。関係KIN=((kinA+kinB-1)%260)+1。音のリズム(`toneRhythm`): 同じ音/和が14=神秘の音/差が5の倍数=同じ導き/隣/1と13/同じ段階/違う段階。四つの視点は関係名・色・音から決める固定ルール(根拠文を併記、優劣ではないと明記)。次の「二人の日」は今日から最大260日先まで `kinOf` を走査。
- **マヤ暦のKIN**: ドリームスペル(13の月の暦)系。基準 2012-12-21 = KIN207、2月29日はKINを進めない(前日と同じ)。検証: 2013-07-26 = KIN164。太陽の紋章 = (KIN-1)%20+1、銀河の音 = (KIN-1)%13+1。ガイドキンは音1・6・11=同じ紋章、2・7・12=+12、3・8・13=+4、4・9=+16、5・10=+8。類似=19−紋章、反対=+10、神秘=21−紋章(音は14−音)。絶対反対=KIN+130、鏡の向こう=261−KIN。古典マヤ暦(GMT相関)とは数え方が異なる旨を結果内で明記。黒キン(GAP)は未実装。

## 10. デザインシステム — 「月光ミスティック」(2026-09 全面刷新)

> コンセプト: 深夜の天体観測所。静謐で厳かな神秘体験。「本物の占い師に鑑定してもらっている」没入感。

| トークン | 値 |
|---|---|
| 背景 | `--midnight-navy #0a0e27`(メイン)/ `--deep-navy #050817`(より深い) |
| 紫 | `--nebula-purple #6b4a8c`(アクセント)/ `--mystic-violet #4a3a6b`(サブ)/ `--glow-purple rgba(107,74,140,.4)`(ホバーグロー) |
| 光 | `--moonlight-silver #c8d4e8`(本文)/ `--star-white #f5f7ff`(見出し)/ `--divine-gold #d4af37` / `--soft-gold #b8941f` |
| 境界 | `--hairline rgba(200,212,232,.15)` |
| 書体 | 見出し(日本語): Shippori Mincho B1 / 欧文見出し: Cormorant Garamond, Cinzel(大文字・字間 .3em)/ 本文: Noto Serif JP(行間1.9・字間.08em) |
| モーション | 600〜1200ms、`--ease-expo cubic-bezier(.19,1,.22,1)`。ホバーは purple glow がゆっくり広がる |

- 既存部品が参照する旧トークン(`--bg` `--ink` `--indigo` `--gold-bright` 等)は `:root` で新パレットへ再割当してある。新規CSSは新トークン名を使うこと。
- `css/style.css` 末尾の「月光ミスティック層」が後勝ちで部品を再着色する。画像素材は `--asset-*` トークン経由で参照(`assets/README.md`)。
- **素材06〜09はすべてオリジナルSVG**(`js/icons.js` の `MysticIcons`、44種)。`mysticIcon(type, opts)` でインライン描画し currentColor で着色。`scripts/build-icons.mjs` が単体 .svg を `assets/` に書き出す。一覧は `/icons-preview.html`(noindex)。
  月相8(`moonPhaseKey()`)/ 天体10 / 星座12(`zodiacKey(month, day)`)/ 五行5 / 角飾り4 / 区切り3 / フレーム2。
- `js/mystic.js`(app.jsより先に読み込む): 金の粒子(canvas)/ カーソルの光 / スクロール出現(`.reveal` → `.in-view`、動的DOMにも MutationObserver で適用)/ ヒーロー視差 / 月相SVG `moonSVG()` / 観測演出の相(`.obs-overlay[data-phase]` = stars → elements → moon)/ タロット表示時の金の粒子 `goldDust()` / 環境音(WebAudio合成・デフォルトOFF・右上トグル・`myouriscope:sound`)。
- 観測演出(`observeThen`)は1相 2.1秒 × 最大3相 = 約6〜7秒。完了時に `myouriscope:chime` イベント。
- 四柱推命の結果に「五行のバランス」(`gogyoBalanceHtml`: 三柱の干支6文字+本命星の五行を集計、光の強さで表示、中央に日主)。
- 相性診断の結果に二人の星座グリフと月相プログレス。ホロスコープの出生図SVGは各サイン領域がホバーでハイライト。
- 禁止(継続): 断定・脅し・商業的な煽り。テキスト可読性最優先(本文は必ず `--moonlight-silver` 以上の明度)。
- 横幅は必ず端末幅以内(`html,body { overflow-x: clip }`)。ヒーローのみ `100vw` で全幅。

## 11. localStorage キー

| キー | 内容 |
|---|---|
| `fortuna:profile` | 名前・生年月日・テーマ |
| `fortuna:daily:<date>` | 今日の一枚(1日1回制御) |
| `fortuna:obsday` | 当日観測済みフラグ |
| `fortuna:collection` / `fortuna:history` | 称号図鑑 / 鑑定履歴(30件) |
| `fortuna:visits` | 連続日数(ストリーク) |
| `fortuna:shufmode` | シャッフル方式(field/stack) |
| `fortuna:journal` | タロット・ジャーナル(託宣の記録、60件) |
| `fortuna:windows` | 月の間・宙の窓の消化記録(1日/一夜1回制御) |
| `fortuna:bp` | 出生地(都道府県index) |
| 引き継ぎ | マイページの引き継ぎコード(export/import) |

## 12. 流入(SEO・シェア計測)

> SPAはハッシュルーティングのため、検索エンジンからは「1ページのサイト」に見える。
> 検索の入口は静的LPが担い、本体アプリへは `/#tarot` 等のハッシュリンクで送客する。

- **検索流入用LP(静的HTML)**: `/today/` `/tarot/` `/horoscope/` `/shichusuimei/` `/aisho/` `/shogo/`。
  各ページ固有の title / description / canonical / OGP+本文+FAQ。構造化データは WebPage / BreadcrumbList / FAQPage。
  スタイルは `css/style.css` を共用し、LP差分だけ `css/lp.css`。生成元テンプレはなくHTML直編集(構成は6ページ共通)。
- **12星座ページ**: `/seiza/`(一覧ハブ・ItemList構造化データ)+ `/seiza/aries/`〜`/seiza/pisces/` の12本。
  日付範囲・エレメント・支配星・気質テキストは `js/data.js` の `ZODIAC` と一致させること(ずれると本体と矛盾する)。
  相性の記述はエレメント(火↔風、地↔水)ベース+4層診断への誘導。
- **タロット78枚の意味ページ**: `/tarot/cards/`(一覧ハブ・ItemList)+ `/tarot/cards/{slug}/` の78本。
  大アルカナ22枚は `TAROT_SLUGS` のslug(fool等)、小アルカナ56枚は `{suit}_{rank}`(wands_ace等)。
  本文(正位置up・逆位置rev・助言advice・大アルカナのテーマ別)は `js/data.js` を Node で評価して抽出し、
  二重管理を避けている(手打ちしない=文言ずれゼロ)。画像は本体と同じ `images/tarot/` を参照。
  ロングテール検索(「◯◯ タロット 意味」「◯◯ 逆位置」)の受け皿。
- **サイト全体のSEO**: `robots.txt`(tarot-debug.html除外)/ `sitemap.xml`(LP追加時はここに追記)/ `404.html`(noindex・主要ページへの導線)。
  トップは canonical+WebSite/Organization/WebApplication の構造化データ。
- **内部リンク**: トップのfooterに「Guide」リンク列(クローラが辿れる実アンカー)。LP側はヘッダー/フッター/関連グリッドで相互リンク。
- **シェア計測**: 招待URL(`buildInviteUrl`)に `utm_source=invite&utm_medium=share` を付与。GA4で友達招待経由の流入を区別できる。
- **キャッシュバスト**: workflowの置換対象は全HTML(`find . -name "*.html" ... sed`)。LPを足すときは `?v=dev` を付けてCSS/JSを参照する。
- **文言ルール**: LPでも「無料」等の商業的な煽り表現は使わない。「登録不要」「データは端末の中だけ」は事実の説明としてOK。

## 13. パフォーマンス

- **CSSの装飾背景は軽量版を使う**: `.bento-art` や `.spread-opt` 等の背景に使う絵札は
  `images/tarot/bg/`(560px幅・quality50)を参照する。原寸(`images/tarot/*.webp`)は
  カードの絵札そのもの(儀式のリビール・/tarot/cards/ ページ)専用。
  新しく絵札をCSS背景に使うときも必ずbg版を作ること(原寸は1枚260〜320KBある)。
- **カード裏面画像のプローブは遅延**: `tarot_back.webp`(164KB)の存在確認は
  `probeTarotBack()`(app.js)で、load後のアイドル時 or 裏面が実際に描画される瞬間に実行。
  起動時の同期ダウンロードに戻さないこと。
- **モバイルnavは現在地に追従**: `navigate()` がアクティブなnavボタンを中央にスクロールする。
  LP側は各ページ末尾のインラインスクリプトが同じ処理を行う(生成テンプレに含まれる)。

## 14. 開発運用メモ

- **監修ループ**: スクリーンショット一式→視点の異なるレビューエージェント(初心者目線/ビジュアル/コピー)→指摘反映→再検証、を繰り返す。
- **検証**: Playwright(`/opt/pw-browsers`)でE2E。フォームの生年月日は `bd-select` の各selectに値を入れて `change`(**bubbles: true 必須**)。
- **ロジック検証**: data.js+fortune.js を `new Function` で評価して Node 単体テスト(分布シミュレーション等)。
- コミットは日本語メッセージ。デバッグ用 `tarot-debug.html`(78枚+裏面の読み込み確認、noindex)。
